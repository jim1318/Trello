<a href="#" class="close_pop">X</a>
<h1 class="pop">Move Card</h1>

<h2>Select New List</h2>
<select name="list" id="list">
  {{#each lists}}
    <option value="{{this.id}}">{{this.name}}</option>
  {{/each}}
</select>

<div class="button_holder">
  <a href="#" id="move_card" class="button green_button">Move</a>
</div>